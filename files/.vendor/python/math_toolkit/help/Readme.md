Runtime help rendering and documentation topic registration live here.

- `runtime.py` renders help output and resolves documentation topics.
- `topics.py` attaches documentation topic metadata to runtime types.
- `config.py` stores runtime documentation link configuration.
- `math_toolkit.util.jupyter_document_link(...)` provides the shared Jupyter
  document-link builder used by rendered help output.
